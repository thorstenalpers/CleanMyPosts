<!--
  Deliberately empty: on this route the host narrows the chrome webview to the sidebar and
  shows the site webview beside it, so nothing in `main` is on screen. The YouTube panel
  itself renders as the sidebar's subnav in `+layout.svelte`.
-->
