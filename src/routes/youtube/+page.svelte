<!--
  Deliberately empty: on this route the host narrows the chrome webview to the sidebar plus
  the action rail and shows the site webview beside them, so nothing in `main` is on screen.
  The YouTube rail itself renders in `+layout.svelte`.
-->
